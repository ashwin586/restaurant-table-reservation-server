// import { dashboardDetails } from "../../../usecases/partnerUseCases/partnerDashboardUseCases.js";

// export const fetchAllDetails = async (req, res) => {
//   try {
//     const result = await dashboardDetails(req.token.number);
//     if (result) {
//       return res.status(200).json({
//         totalRevenue: result.totalRevenue,
//         totalRestaurants: result.totalRestaurants,
//         totalReviewCount: result.totalReviewCount,
//         totalBookingCount: result.totalBookingCount,
//         chartData: result.chartDetails
//       });
//     }
//   } catch (err) {
//     console.log(err);
//   }
// };
